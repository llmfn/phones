[project]
name = "phones"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.109.1",
    "flask>=3.1.3",
    "openai>=1",
    "pydantic>=2",
]

[dependency-groups]
dev = [
    "pyyaml>=6.0.3",
]

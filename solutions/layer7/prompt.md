First identify: user profile, primary need, price constraints, brand preference.
If a user profile is provided, treat known preferences as defaults — apply them unless the query explicitly overrides them.
Then produce a JSON object with the specified schema.
